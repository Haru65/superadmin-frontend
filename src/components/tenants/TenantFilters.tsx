import { Search } from 'lucide-react'
import { Input } from '@/components/ui/input'
import type { TenantStatus, TenantType } from '@/types/superadmin'

export type TenantFilterState = { search: string; type: 'all' | TenantType; status: 'all' | TenantStatus }
export const TenantFilters = ({ value, onChange }: { value: TenantFilterState; onChange: (value: TenantFilterState) => void }) =>
  <div className="grid gap-3 rounded-2xl border border-slate-200 bg-white p-4 shadow-panel md:grid-cols-[1fr_180px_180px]"><div className="relative"><Search className="absolute left-3 top-3 h-4 w-4 text-slate-400" /><Input className="pl-9" placeholder="Search name, slug, email, phone..." value={value.search} onChange={(event) => onChange({ ...value, search: event.target.value })} /></div><select className="h-10 rounded-lg border border-slate-200 bg-white px-3 text-sm" value={value.type} onChange={(event) => onChange({ ...value, type: event.target.value as TenantFilterState['type'] })}><option value="all">All business types</option><option value="cafe">Cafe</option><option value="restaurant">Restaurant</option><option value="lodging">Lodging</option></select><select className="h-10 rounded-lg border border-slate-200 bg-white px-3 text-sm" value={value.status} onChange={(event) => onChange({ ...value, status: event.target.value as TenantFilterState['status'] })}><option value="all">All statuses</option><option value="active">Active</option><option value="paused">Paused</option><option value="suspended">Suspended</option><option value="inactive">Inactive</option></select></div>
